server:
  port: 8080

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui/
  api-docs:
    enabled: true
    path: /docs/

management:
  endpoints:
    web:
      exposure:
        include: health, info, caches
